server:
  port: 8084

spring:
  application:
    name: maidc-task
  datasource:
    url: jdbc:postgresql://localhost:5432/maidc?currentSchema=system
    username: maidc
    password: maidc123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        default_schema: system
        dialect: org.hibernate.dialect.PostgreSQLDialect
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: dev
