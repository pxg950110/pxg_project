server:
  port: 8086

spring:
  application:
    name: maidc-audit
  datasource:
    url: jdbc:postgresql://localhost:5432/maidc?currentSchema=audit&stringtype=unspecified
    username: maidc
    password: maidc123
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        default_schema: audit
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
      password: maidc123
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: 5672
    username: maidc
    password: maidc123
    virtual-host: ${RABBITMQ_VHOST:/}
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:localhost:8848}
        username: nacos
        password: nacos
      config:
        import-check:
          enabled: false

maidc:
  jwt:
    secret: maidc-jwt-secret-key-2026-change-in-production

logging:
  level:
    com.maidc: INFO
