# MAIDC UI 细化设计 — 模型管理 + CDR 数据管理

> **版本**: v1.1
> **日期**: 2026-04-09
> **状态**: 设计评审中
> **模块**: 模型管理（Phase 1 核心）+ CDR 临床数据管理
> **方法**: 渐进式细化 — 按模块逐个完成

---

## 1. 设计范围

### 1.1 现有页面细化（10页）

| # | 页面 | ID | 细化程度 |
|---|------|-----|---------|
| 03 | 模型列表 | `Syc8a` | 补充筛选/排序/分页/视图切换 |
| 04 | 模型详情 | `7xGUL` | 补充3个空Tab内容（版本/评估/部署） |
| 05 | 部署监控 | `l2c3F` | 补充时间选择器/多指标Tab |
| 05a | 部署详情 | `upipB` | 补充实例详情列/推理日志入口 |
| 06 | 版本管理 | `ybnMi` | 修复版本号一致性/补充对比详情 |
| 07 | 模型评估 | `NyVaz` | 补充筛选/ROC曲线/导出按钮 |
| 07a | 评估详情 | `gBEhi` | 补充ROC曲线/错误分析/数据集卡片 |
| 08 | 审批流程 | `DpQ4p` | 大幅补充：审批列表表格+3个Tab |
| 08a | 审批详情 | `zBiWN` | 补全时间线详情/审批材料卡片 |
| 09 | 流量路由 | `eEgv3` | 补充路由历史/变体指标对比 |

### 1.2 新增页面（3项）

| 页面 | 说明 |
|------|------|
| 推理日志 | 日志表格+时间范围筛选+JSON展开 |
| AI Worker 管理 | Worker节点列表+GPU资源总览 |
| 创建路由弹窗 | 路由配置表单（CANARY/AB/WEIGHTED/MIRROR） |

### 1.3 弹窗细化（6个）

| 弹窗 | ID | 细化内容 |
|------|-----|---------|
| 注册模型 | `cGet2` | 补充 input/output schema、tags、所属项目字段 |
| 编辑模型 | `W7KS3` | 完善所有字段预填充 |
| 上传版本 | `VQF32` | 补充 hyper_params、训练数据集选择 |
| 新建评估 | `KsKs6` | 补充 eval_type 选择、metrics_config |
| 审批操作 | `Uo48v` | 保持现有设计 |
| 新建部署 | `kH2Qv` | 补充 serving_framework、env_vars、auto_scale 配置 |

---

## 2. 组件库升级

### 2.1 新增组件

| 组件名 | 说明 | 使用场景 |
|--------|------|---------|
| StatusBadge/Model | DRAFT/REGISTERED/PUBLISHED/DEPRECATED | 模型列表、详情 |
| StatusBadge/Version | CREATED/TRAINING/EVALUATING/APPROVED/DEPLOYED/DEPRECATED | 版本管理 |
| StatusBadge/Deployment | CREATING/RUNNING/STOPPING/STOPPED/FAILED | 部署监控 |
| StatusBadge/Eval | PENDING/RUNNING/COMPLETED/FAILED | 评估管理 |
| StatusBadge/Approval | PENDING/APPROVED/REJECTED | 审批流程 |
| EmptyState | 空数据占位+图标+引导文案 | 所有列表页 |
| FileUploader | 拖拽上传区域+文件列表 | 版本上传、审批材料 |
| StepIndicator | 3-4步骤条 | 模型注册向导 |
| JsonViewer | 格式化JSON展示+语法高亮 | Schema、评估详情 |
| TrafficBar | 多色流量分配条 | 流量路由 |
| Timeline | 竖向时间线+节点状态 | 审批详情 |
| MetricChart | 图表占位区（标注图表类型） | 监控页、评估详情 |
| KeyValueEditor | 键值对动态编辑器 | 部署配置、环境变量 |

### 2.2 状态配色规范

```
模型状态:
  DRAFT      → 灰色 (#8B95A5 bg: #F1F3F5)
  REGISTERED → 蓝色 (#3B82F6 bg: #EFF6FF)
  PUBLISHED  → 绿色 (#10B981 bg: #ECFDF5)
  DEPRECATED → 黄色 (#F59E0B bg: #FFFBEB)

版本状态:
  CREATED    → 灰色
  TRAINING   → 蓝色（脉动动画）
  EVALUATING → 黄色
  APPROVED   → 绿色
  DEPLOYED   → 紫色 (#8B5CF6 bg: #F5F3FF)
  DEPRECATED → 灰色

部署状态:
  CREATING   → 蓝色（脉动）
  RUNNING    → 绿色
  STOPPING   → 黄色
  STOPPED    → 灰色
  FAILED     → 红色 (#EF4444 bg: #FEF2F2)

评估状态:
  PENDING    → 灰色
  RUNNING    → 蓝色（脉动）
  COMPLETED  → 绿色
  FAILED     → 红色

审批状态:
  PENDING    → 黄色
  APPROVED   → 绿色
  REJECTED   → 红色

告警状态:
  FIRING        → 红色
  ACKNOWLEDGED  → 黄色
  RESOLVED      → 绿色
```

---

## 3. 逐页细化规格

### 3.1 模型列表 (Syc8a)

**改动清单：**

1. 筛选栏增加"基因组"分类 → 变为：全部 | 影像 | NLP | 基因组 | 结构化 | 多模态
2. 增加排序下拉：按更新时间（默认）/ 按名称 / 按QPS / 按创建时间
3. 底部分页器：显示"共 X 个模型"，每页 12/24/48 切换
4. 右上角增加视图切换图标：卡片视图 | 列表视图
5. 状态标签使用 StatusBadge/Model 组件
6. 卡片底部QPS标签：未部署时显示"未部署"灰色文字代替QPS数值

**示例数据（保持现有6张卡片，补充）：**
- 第4张"基因变异分类器"的QPS改为"QPS: 12"（已有，保持）
- 确保筛选"基因组"时能命中此卡片

---

### 3.2 模型详情 (7xGUL)

**改动清单：**

1. **基本信息 Tab**（已有内容，微调）
   - 模型编码改为 `LUNG_NODULE_DET_001`（对齐 PRD model_code 格式）
   - 保持现有布局不变

2. **版本列表 Tab**（新增）
   - 表格列：版本号 / 描述 / 框架版本 / 文件大小 / 训练指标摘要 / 状态 / 创建时间 / 操作
   - 示例数据（5行）：
     ```
     v2.3.1 | 新增3D卷积+注意力机制 | PyTorch 2.1 | 520MB | AUC:0.983 | APPROVED | 2026-04-07
     v2.2.0 | 优化小结节检测       | PyTorch 2.0 | 500MB | AUC:0.923 | DEPLOYED | 2026-03-15
     v2.1.0 | 新增多尺度特征融合    | PyTorch 2.0 | 490MB | AUC:0.908 | DEPLOYED | 2026-02-20
     v2.0.0 | 架构升级ResNet50     | PyTorch 1.13| 480MB | AUC:0.895 | DEPRECATED | 2026-01-10
     v1.0.0 | 初始版本             | PyTorch 1.13| 475MB | AUC:0.882 | DEPRECATED | 2025-11-01
     ```
   - 版本对比区：两个版本下拉选择器 + "对比"按钮
   - 对比结果表格：指标 / v2.3.1 / v2.2.0（行：AUC, Accuracy, Recall, Precision, F1, 参数量, 文件大小）

3. **评估记录 Tab**（新增）
   - 评估卡片列表（3张）：
     - v2.3.1 外部验证集评估（COMPLETED，AUC:0.987, Acc:96.8%）
     - v2.3.1 内部回归测试（COMPLETED，AUC:0.983）
     - v2.2.0 性能基准测试（COMPLETED，AUC:0.923）
   - 每张卡片：评估名称 / 评估类型标签(内部/外部/交叉) / 数据集名 / 核心指标 / 耗时 / 状态 / "查看报告"按钮

4. **部署管理 Tab**（新增）
   - 部署表格列：部署名称 / 版本 / 类型 / 框架 / 状态 / QPS / 延迟 / 操作
   - 示例数据（3行）：
     ```
     肺结节检测-生产 | v2.2.0 | ONLINE | TRITON | RUNNING(绿) | 56 | 23ms | 管理
     肺结节检测-灰度 | v2.3.1 | ONLINE | TRITON | RUNNING(绿) | 5  | 25ms | 管理
     肺结节检测-测试 | v2.3.1 | ONLINE | FASTAPI | STOPPED(灰) | -  | -    | 管理
     ```
   - 右上角"新建部署"按钮

---

### 3.3 部署监控 (l2c3F)

**改动清单：**

1. 增加时间范围选择器：近1h | 近6h | 近24h | 近7d
2. 增加自动刷新指示器："自动刷新 30s" + 绿色圆点
3. QPS图表区域增加Tab切换：QPS | 延迟P50/P99 | GPU利用率 | 错误率
4. 告警表格"操作"列增加"确认"按钮（FIRING 状态行）
5. 部署状态卡片增加快速链接到部署详情
6. 状态标签统一使用新配色

---

### 3.4 部署详情 (upipB)

**改动清单：**

1. 实例表格增加列：运行时长 / 错误数(24h) / 内存使用
   ```
   实例ID | QPS | 延迟 | GPU使用率 | 内存使用 | 错误数 | 运行时长 | 状态
   ```
2. 增加"推理日志"按钮（在操作栏中）
3. 增加"重启"按钮（在扩容、下线旁边）
4. 部署配置卡片增加资源使用进度条：
   - CPU: 3.2/4 核 (80%)
   - GPU: 7.2/8 GB (90%)
   - 内存: 12/16 GB (75%)

---

### 3.5 版本管理 (ybnMi)

**改动清单：**

1. **修复版本号一致性**：统一使用 v2.3.1 为最新版本，序列为 v1.0.0 → v2.3.1
2. 版本对比区补充具体指标对比行：
   - 超参数对比：learning_rate, batch_size, epochs, optimizer
   - 训练指标对比：loss, accuracy, val_loss, val_accuracy
   - 评估指标对比：auc, f1, precision, recall
3. 操作列根据状态显示不同按钮：
   - CREATED: [评估] [编辑]
   - EVALUATING: [查看进度]
   - APPROVED: [部署]
   - DEPLOYED: [查看部署]
   - DEPRECATED: [查看]
4. 状态标签使用 StatusBadge/Version 组件

---

### 3.6 模型评估 (NyVaz)

**改动清单：**

1. 增加筛选栏：
   - 评估类型下拉：全部 / 内部评估 / 外部验证 / 交叉验证
   - 状态下拉：全部 / 进行中 / 已完成 / 已失败
   - 关键词搜索
2. 评估卡片增加评估类型标签（蓝=内部，紫=外部，橙=交叉验证）
3. 展开的评估详情面板增加 ROC 曲线占位区（灰色方框 + "ROC曲线" 标注）
4. 已完成的评估卡片增加"导出报告"按钮
5. 运行中评估的进度条显示百分比文字（如 "67%"）

---

### 3.7 评估详情 (gBEhi)

**改动清单：**

1. 增加 ROC 曲线可视化区域（占左侧下方）：
   - 灰色占位方框，标注 "ROC曲线 (AUC=0.987)"
   - X轴：1-Specificity, Y轴：Sensitivity
   - 对角虚线参考线
2. 增加"错误分析"区域（评估配置卡片下方）：
   - 标题"错误分析"
   - JsonViewer 组件展示 error_analysis JSON
3. 增加"数据集信息"卡片（评估配置内）：
   - 数据集名称：CT肺结节数据集v2
   - 样本数量：1,200 例
   - 数据集版本：v2.1
4. 增加"下载报告"按钮（右上角操作区）
5. 指标卡片增加趋势指示：
   - 准确率 96.5% ↑0.3%（绿色向上箭头）
   - 灵敏度 94.2% ↓0.1%（红色向下箭头）
   - 精确率 93.8% ↑0.5%
   - F1 0.94 ↑0.2%

---

### 3.8 审批流程 (DpQ4p)

**改动清单（大幅补充）：**

1. **"待审批"Tab**：审批请求表格
   - 列：模型名称 / 版本 / 审批类型 / 申请人 / 提交时间 / 状态 / 操作
   - 示例数据（5行）：
     ```
     肺结节检测 | v2.3.1 | 上线审批(DEPLOY) | 张医生 | 2026-04-09 10:00 | PENDING(黄) | 审批
     病理分类   | v3.0.0 | 发布审批(PUBLISH) | 李工   | 2026-04-08 16:30 | PENDING(黄) | 审批
     心电异常   | v1.5.1 | 临床使用(CLINICAL_USE) | 王医生 | 2026-04-08 09:15 | PENDING(黄) | 审批
     NLP实体   | v1.0.0 | 上线审批(DEPLOY) | 赵工   | 2026-04-07 14:00 | PENDING(黄) | 审批
     基因分类   | v3.1.0 | 发布审批(PUBLISH) | 陈博士 | 2026-04-06 11:30 | PENDING(黄) | 审批
     ```
   - 右上角显示"待审批 5 项"计数

2. **"已审批"Tab**：已完成的审批记录
   - 列同上 + 增加审批人 / 审批时间 / 结果
   - 示例数据混合 APPROVED 和 REJECTED 状态

3. **"全部"Tab**：所有审批记录
   - 增加筛选：按审批类型 / 按结果 / 时间范围

---

### 3.9 审批详情 (zBiWN)

**改动清单：**

1. 补全时间线4个步骤：
   - Step 1: 技术评审 — 王工 — 2026-04-07 11:00 — APPROVED — "模型性能达标，技术架构合理"
   - Step 2: 临床评审 — 刘主任 — 2026-04-07 14:30 — APPROVED — "临床验证结果良好，建议上线"
   - Step 3: 管理审批 — 李主任 — 进行中 — PENDING
   - Step 4: 完成 — 待定

2. 增加"审批材料"卡片（关联模型卡片下方）：
   - 评估报告 v2.3.1 (EVALUATION) [下载]
   - 临床验证报告 (CLINICAL) [下载]

3. 增加"风险评估"文本区域：
   - 显示 risk_assessment 字段内容
   - 增加"临床验证描述"子区域显示 clinical_validation

---

### 3.10 流量路由 (eEgv3)

**改动清单：**

1. 展开详情增加"各变体实时指标"区域：
   - 对比表格：部署名 | QPS | 延迟P99 | 错误率 | 请求量
   - 生产v2.2.0: 50.4 | 22ms | 0.1% | 90%
   - 灰度v2.3.1: 5.6 | 25ms | 0.2% | 10%

2. 增加"路由历史"区域：
   - 最近变更记录列表：时间 / 操作人 / 变更描述
   - 3-4行历史数据

3. JSON 预览使用 JsonViewer 组件（带语法高亮）
4. "禁用"状态的卡片降低不透明度至60%

---

## 4. 新增页面规格

### 4.1 推理日志页

**导航位置**：模型管理 → 部署监控 → [具体部署] → 推理日志

**页面结构：**

```
┌─ 标题栏 ────────────────────────────────────────────────┐
│ ← 推理日志 · 肺结节检测-生产                             │
│ 部署编号: DEP-2026-0008                                  │
├─ 筛选栏 ────────────────────────────────────────────────┤
│ 时间范围: [近1h ▼]  状态: [全部 ▼]  [查询] [重置]        │
├─ 统计卡片 ──────────────────────────────────────────────┤
│ 总请求: 12,456  成功率: 99.7%  平均延迟: 23ms  超时: 3    │
├─ 日志表格 ──────────────────────────────────────────────┤
│ 请求ID | 患者ID | 置信度 | 延迟 | 版本 | 调用方 | 状态 | 时间│
│ req-xxx | P-001 | 0.95  | 18ms | v2.2 | Portal | SUCCESS | ..│
│ req-yyy | P-002 | 0.78  | 45ms | v2.2 | HIS    | SUCCESS | ..│
│ ...                                                       │
├─ 展开行 ───────────────────────────────────────────────┤
│ 输入摘要 (JsonViewer) | 推理结果 (JsonViewer)              │
├─ 分页 ──────────────────────────────────────────────────┤
│ 共 12,456 条  < 1 2 3 ... 623 >                          │
└──────────────────────────────────────────────────────────┘
```

**表格列**：request_id / patient_id / encounter_id / confidence / latency_ms / model_version_no / caller_service / status / created_at

**可展开行**：input_summary (JSON) + output_result (JSON)

---

### 4.2 AI Worker 管理

**导航位置**：模型管理 → AI Worker

**页面结构：**

```
┌─ 标题栏 ────────────────────────────────────────────────┐
│ AI Worker 集群管理                                       │
│ 管理 GPU 推理集群的 Worker 节点和任务队列                    │
├─ 资源总览卡片 ──────────────────────────────────────────┤
│ 活跃节点: 4/6  GPU总数: 8  GPU使用率: 67%  运行任务: 12     │
├─ Worker 节点列表 ──────────────────────────────────────┤
│ 节点名 | GPU型号 | GPU使用 | 内存使用 | 运行任务 | 状态 | 操作│
│ worker-gpu-01 | A100 40GB | 85% | 32/40GB | 3 | 运行中 | 详情│
│ worker-gpu-02 | A100 40GB | 72% | 28/40GB | 2 | 运行中 | 详情│
│ worker-gpu-03 | T4 16GB   | 45% | 10/16GB | 1 | 运行中 | 详情│
│ worker-cpu-01 | - | - | 8/16GB | 4 | 运行中 | 详情│
│ worker-gpu-04 | A100 40GB | 0%  | 0/40GB  | 0 | 离线   | 重启│
│ worker-gpu-05 | T4 16GB   | 0%  | 0/16GB  | 0 | 离线   | 重启│
├─ 任务队列状态 ──────────────────────────────────────────┤
│ 队列名 | 类型 | 待处理 | 运行中 | 优先级                  │
│ inference | 推理 | 5 | 8 | 高                         │
│ evaluation | 评估 | 12 | 3 | 中                        │
│ preprocessing | 预处理 | 23 | 1 | 低                   │
│ batch_inference | 批量推理 | 0 | 0 | 低                │
└──────────────────────────────────────────────────────────┘
```

---

### 4.3 创建路由弹窗

**弹窗结构（宽度 560px）：**

```
┌─ 新建路由规则 ─────────────────────────────────────────┐
│                                                         │
│ 路由名称 * [                          ]                 │
│ 目标模型 * [肺结节检测模型 ▼]                             │
│ 路由类型 * [金丝雀发布 ▼]                                 │
│   选项: 金丝雀发布 / A/B测试 / 加权路由 / 流量镜像         │
│                                                         │
│ ── 流量配置 ────────────────────────────────────────── │
│ 默认部署 * [肺结节检测-生产(v2.2.0) ▼]                   │
│ 灰度部署 * [肺结节检测-灰度(v2.3.1) ▼]                   │
│                                                         │
│ 金丝雀流量比例 [10] %                                     │
│ 成功率阈值 [95] %                                        │
│ 自动提升 [关闭]                                          │
│                                                         │
│ ── 高级配置 ────────────────────────────────────────── │
│ 持续观察时间 [300] 秒                                     │
│ 回退阈值 [90] %                                          │
│                                                         │
│                      [取消]  [创建路由]                   │
└─────────────────────────────────────────────────────────┘
```

**条件显隐**：路由类型选择不同时，流量配置区显示不同字段：
- 金丝雀：灰度部署 + 百分比 + 阈值 + 自动提升
- A/B测试：部署A + 部署B + 50/50分配
- 加权路由：N个部署 + 各自权重百分比
- 流量镜像：源部署 + 镜像部署

---

## 5. 弹窗细化规格

### 5.1 注册模型弹窗 (cGet2)

**补充字段：**

现有基础上增加：
- **所属项目**：下拉选择（关联研究项目，可选）
- **Input Schema**：JsonViewer 编辑模式，预填示例：
  ```json
  {"type": "object", "properties": {"image": {"type": "file", "format": "dicom/png"}}}
  ```
- **Output Schema**：JsonViewer 编辑模式，预填示例：
  ```json
  {"type": "array", "items": {"type": "object", "properties": {"bbox": {}, "confidence": {}, "class": {}}}}
  ```
- **标签**：标签输入框，支持多个标签（输入后回车添加）

考虑改为**多步向导**（StepIndicator）：
- Step 1: 基本信息（名称、编码、类型、框架、任务类型）
- Step 2: Schema定义（input/output schema）
- Step 3: 补充信息（描述、标签、所属项目、许可证）

### 5.2 上传版本弹窗 (VQF32)

**补充字段：**

- **超参数 (hyper_params)**：KeyValueEditor 组件
  - 预填示例行：learning_rate=0.001, batch_size=32, epochs=100, optimizer=Adam
- **训练数据集 (training_dataset_id)**：下拉选择
  - 占位符："选择关联训练数据集"

### 5.3 新建评估弹窗 (KsKs6)

**补充字段：**

- **评估类型 (eval_type)**：下拉选择
  - 选项：内部评估 / 外部验证 / 交叉验证
- **评估指标 (metrics_config)**：复选框组
  - 选项：Accuracy / Recall / Precision / F1 / AUC / Sensitivity / Specificity（默认全选）
- **置信度阈值**：数字输入（默认 0.5）
- **IoU阈值**：数字输入（默认 0.5，仅目标检测/分割任务显示）

### 5.4 新建部署弹窗 (kH2Qv)

**补充字段：**

- **推理框架 (serving_framework)**：下拉选择
  - 选项：TRITON / TORCHSERVE / TFSERVING / ONNX_RUNTIME / FASTAPI
- **环境变量 (env_vars)**：KeyValueEditor 组件
  - 预填示例：MODEL_PATH=/models/lung-nodule
- **自动扩缩容**：开关组件
  - 开启时显示：最小副本数 [1] / 最大副本数 [3]

---

## 6. 版本号一致性修正

**当前问题**：不同页面中同一模型的版本号不统一

**统一方案**（以肺结节检测模型为例）：

| 版本 | 发布日期 | 关键变化 | 状态 |
|------|---------|---------|------|
| v1.0.0 | 2025-11-01 | 初始版本，ResNet18骨干网络 | DEPRECATED |
| v1.1.0 | 2025-12-15 | 修复推理BUG，优化预处理流程 | DEPRECATED |
| v1.2.0 | 2026-01-20 | 新增3D卷积模块，提升小结节检测 | DEPRECATED |
| v2.0.0 | 2026-02-10 | 架构升级ResNet50，重新训练 | DEPRECATED |
| v2.1.0 | 2026-03-01 | 多尺度特征融合，FPN优化 | DEPLOYED（生产环境） |
| v2.2.0 | 2026-03-15 | 新增注意力机制，小结节精度提升 | DEPLOYED（灰度环境） |
| v2.3.1 | 2026-04-07 | 3D卷积+注意力融合，最终优化 | APPROVED（待部署） |

**同步更新**：所有涉及版本号的页面统一使用此序列。

---

## 7. 数据一致性要求

- 模型名称统一为"肺结节检测模型"（代码 LUNG_NODULE_DET_001）
- 卡片/详情/部署中引用的版本号需完全一致
- 评估指标数值：AUC 0.983 / Accuracy 96.8% / Sensitivity 94.5% / Specificity 97.2%
- 部署信息：生产环境 v2.1.0 (QPS:56), 灰度环境 v2.2.0 (QPS:5)
- 审批关联的版本号为 v2.3.1

---

## 8. CDR 临床数据管理模块

### 8.1 现有页面细化（4页）

#### 8.1.1 患者列表 (c2K7C)

**改动清单：**

1. 增加高级筛选区域（可展开/折叠）：
   - 患者ID搜索
   - 诊断关键词（支持ICD-10编码搜索）
   - 入院日期范围选择器
   - 数据来源系统筛选
2. 增加"批量导出"按钮（蓝色轮廓，点击后弹出导出审批弹窗）
3. "来源系统"列改为标签样式：
   - HIS → 蓝色标签
   - PACS → 紫色标签
   - LIS → 绿色标签
   - EMR → 橙色标签
4. 表格头增加排序指示器（可按患者编号/出生日期排序）
5. "查看详情"操作改为跳转患者360视图页（非就诊详情）

---

#### 8.1.2 就诊详情 (EFPi5) — 7个Tab全部填充

**Tab 1: 基本信息**

| 区域 | 内容 |
|------|------|
| 入院信息卡 | 入院时间 / 出院时间 / 住院天数 / 科室 / 床号 / 入院途径(急诊/门诊/转院) |
| 入院诊断 | 主诊断(ICD-10编码+名称) + 次要诊断列表 |
| 护理信息 | 护理等级(特级/一级/二级/三级) / 责任护士 |
| 过敏史 | 过敏原列表（药物/食物/其他），红底标签样式 |
| 既往病史 | 病史列表（疾病名称 / 确诊时间 / 状态） |

**Tab 2: 诊断记录**

表格列：
```
诊断编码(ICD-10) | 诊断名称 | 诊断类型(主诊断/次诊断/并发症) | 诊断时间 | 诊断医生 | 状态(确认/疑似)
```

示例数据（4行）：
```
J98.4     | 肺部疾患    | 主诊断  | 2026-04-01 | 李医生 | 确认
J84.10    | 肺纤维化    | 次诊断  | 2026-04-02 | 李医生 | 确认
E11.9     | 2型糖尿病   | 并发症  | 2026-04-01 | 王医生 | 确认
I10       | 高血压      | 既往史  | 2020-03-15 | 陈医生 | 确认
```

**Tab 3: 检验结果**

- 检验日期分组（按采样时间分组）
- 每组内检验项目表格：
```
检验项目 | 结果 | 单位 | 参考范围 | 异常标记 | 采样时间
WBC     | 12.5 | 10^9/L | 3.5-9.5 | ↑(红色) | 2026-04-02 08:30
RBC     | 4.2  | 10^12/L| 4.3-5.8 | ↓(红色) | 2026-04-02 08:30
HGB     | 135  | g/L    | 130-175 | 正常    | 2026-04-02 08:30
CRP     | 28.6 | mg/L   | 0-10    | ↑(红色) | 2026-04-02 08:30
```
- 异常项红色高亮，正常项绿色勾

**Tab 4: 影像检查**

影像列表表格：
```
检查类型 | 检查部位 | 检查时间 | 报告状态 | 报告摘要 | 操作
CT平扫   | 胸部    | 2026-04-02 | 已报告 | 双肺纹理增粗，右下肺见磨玻璃结节 | 查看报告/查看影像
X线      | 胸部    | 2026-04-01 | 已报告 | 心影不大，双膈面光滑 | 查看报告/查看影像
```
- "查看影像"链接到PACS查看器（外链图标）

**Tab 5: 用药记录**

用药列表表格：
```
药品名称 | 通用名 | 剂量 | 用法 | 频次 | 开始时间 | 结束时间 | 开嘱医生
头孢曲松钠 | 头孢曲松 | 2g | 静脉滴注 | qd | 2026-04-01 | 2026-04-07 | 李医生
氨溴索   | 氨溴索  | 30mg | 口服  | tid | 2026-04-01 | 2026-04-05 | 李医生
```

**Tab 6: 生命体征**

- 上方：4个指标卡片（最新值）：
  - 体温: 36.8°C / 血压: 125/82mmHg / 心率: 78次/min / 血氧: 98%
- 下方：时间序列图表占位（灰色方框标注"生命体征趋势图"）
  - 可切换指标：体温 / 血压(收缩/舒张) / 心率 / 血氧

**Tab 7: 临床文书**

文书列表表格：
```
文书类型 | 标题 | 创建时间 | 创建者 | 状态 | 操作
入院记录 | 入院记录-张** | 2026-04-01 10:30 | 李医生 | 已签名 | 查看
病程记录 | 日常病程记录 | 2026-04-02 09:00 | 李医生 | 已签名 | 查看
手术记录 | - | - | - | - | -
出院小结 | - | - | - | 未创建 | -
```

---

#### 8.1.3 数据源管理 (kveL5)

**改动清单：**

1. 操作列增加按钮：配置 / 强制同步 / 日志 / 删除（红色）
2. EMR异常行增加错误信息Tooltip（悬停显示"Connection timeout after 30s"）
3. 增加"同步历史"入口按钮（在表格上方）
4. 增加"健康状态"列（绿色圆点=健康，黄色=延迟，红色=异常）

---

#### 8.1.4 数据源详情 (ZqM8J)

**改动清单：**

1. 同步表列表增加分页器（24张表分页展示）
2. 增加"同步历史图表"区域：
   - 灰色图表占位区，标注"近7天同步趋势"
   - 显示同步记录数/延迟/失败数趋势
3. 增加"最近错误日志"卡片：
   - 时间 / 错误类型 / 错误信息 / 重试状态
   - 3-4行错误数据

---

### 8.2 新增页面（4页）

#### 8.2.1 患者360视图

**导航**：数据管理 → 患者列表 → [查看详情]

**页面结构：**

```
┌─ 标题栏 ────────────────────────────────────────────────┐
│ ← 患者档案 · P20260408001                                │
│ 张** | 男 | 58岁 | A型血                                  │
├─ 基本信息卡 ────────────────────────────────────────────┤
│ 出生: 1965-03-12 | 联系方式: 138****5678                  │
│ 身份证: 3101**********3456 | 医保类型: 城镇职工            │
│ 过敏史: 青霉素(红标)                                      │
├─ 关键指标卡片行 ───────────────────────────────────────┤
│ 就诊次数: 12 | 最近就诊: 2026-04-01 | 诊断数: 5 | 关联研究: 2│
├─ Tab栏 ───────────────────────────────────────────────┤
│ 就诊时间线 | 诊断汇总 | 指标趋势 | 关联数据 | AI推理记录     │
├─ Tab内容 ─────────────────────────────────────────────┤
│                                                          │
│ 【就诊时间线】（默认Tab）                                  │
│ 时间线视图：                                              │
│ ● 2026-04-01 ~ 2026-04-08 呼吸内科 住院 [查看详情]        │
│   主诊断: J98.4 肺部疾患                                  │
│ ● 2026-01-15 ~ 2026-01-18 呼吸内科 住院 [查看详情]        │
│   主诊断: J84.10 肺纤维化                                 │
│ ● 2025-09-20 门诊 [查看详情]                              │
│   主诊断: J06.9 急性上呼吸道感染                           │
│ ● ...                                                    │
│                                                          │
│ 【诊断汇总】                                              │
│ 诊断词云/列表 + 统计图表占位                               │
│                                                          │
│ 【指标趋势】                                              │
│ 检验指标趋势图表（可选择指标+时间范围）                     │
│                                                          │
│ 【关联数据】                                              │
│ 患者被纳入的研究项目列表 + 数据集列表                       │
│                                                          │
│ 【AI推理记录】                                            │
│ 该患者所有AI模型推理结果列表                               │
│ 模型名称 / 版本 / 推理时间 / 结果 / 置信度                  │
└──────────────────────────────────────────────────────────┘
```

---

#### 8.2.2 数据字典映射

**导航**：数据管理 → 数据字典

**页面结构：**

```
┌─ 标题栏 ────────────────────────────────────────────────┐
│ 数据字典映射管理                                         │
│ 管理源数据到标准术语体系（ICD-10/LOINC/SNOMED CT）的映射    │
├─ 统计卡片 ──────────────────────────────────────────────┤
│ 标准体系: 3 | 映射规则: 1,245 | 自动映射率: 78% | 待审核: 56│
├─ 标准体系管理区域 ─────────────────────────────────────┤
│ 卡片1: ICD-10 (疾病诊断) - 已导入 68,000 条              │
│ 卡片2: LOINC (检验指标) - 已导入 12,450 条               │
│ 卡片3: SNOMED CT (临床术语) - 已导入 350,000 条          │
├─ 映射规则表格 ─────────────────────────────────────────┤
│ 源系统 | 源字段 | 标准体系 | 目标编码 | 映射类型 | 状态    │
│ HIS   | 诊断名  | ICD-10 | J98.4   | 自动(95%) | 已确认  │
│ HIS   | 检验项  | LOINC  | 33914-3 | 手动     | 已确认  │
│ PACS  | 检查类  | SNOMED | 77477000| 自动(88%) | 待审核  │
│ ...                                                     │
├─ 映射验证区域 ─────────────────────────────────────────┤
│ [批量验证] [导出映射表] [导入映射规则]                     │
└──────────────────────────────────────────────────────────┘
```

---

#### 8.2.3 脱敏规则管理

**导航**：数据管理 → 脱敏管理

**页面结构：**

```
┌─ 标题栏 ────────────────────────────────────────────────┐
│ 数据脱敏管理                                             │
│ 管理患者隐私数据的脱敏策略与导出审批                       │
├─ Tab栏 ────────────────────────────────────────────────┤
│ 脱敏策略 | 导出审批 | 审计记录                            │
├─ 脱敏策略Tab ──────────────────────────────────────────┤
│                                                          │
│ 策略表格:                                                │
│ 字段名 | 数据类型 | 脱敏方式 | 规则说明 | 应用范围        │
│ 姓名   | 文本    | 掩码    | 保留首字+** | 全局           │
│ 身份证 | 文本    | 掩码    | 保留前4后4   | 全局           │
│ 手机号 | 文本    | 掩码    | 保留前3后4   | 全局           │
│ 出生日期 | 日期  | 泛化    | 仅保留年份   | 科研数据       │
│ 住址   | 文本    | 截断    | 仅保留到区级 | 全局           │
│                                                          │
│ [新增策略] [批量导入]                                     │
├─ 导出审批Tab ──────────────────────────────────────────┤
│ 审批表格:                                                │
│ 申请人 | 导出范围 | 用途 | 数据量 | 状态 | 时间 | 操作    │
│ 张医生 | 肺癌队列 | 科研 | 1,200条 | 审批中 | .. | 审批   │
│ 李博士 | 糖尿病组 | 科研 | 800条  | 已通过 | .. | 查看   │
├─ 审计记录Tab ──────────────────────────────────────────┤
│ 数据访问日志表格:                                        │
│ 操作人 | 操作类型 | 数据范围 | 时间 | 审批编号            │
└──────────────────────────────────────────────────────────┘
```

---

#### 8.2.4 就诊列表页

**导航**：患者360视图 → 就诊时间线 → [某次就诊]

或直接：数据管理 → 患者列表 → [查看详情] → 就诊时间线Tab

此页面实际上可以整合在患者360的"就诊时间线"Tab中，如果内容过多再独立成页。

**内容**：
- 患者信息头（复用患者360的头部）
- 就诊列表表格：
  ```
  就诊流水号 | 类型 | 科室 | 入院时间 | 出院时间 | 住院天数 | 主诊断 | 操作
  ENC-20260408-001 | 住院 | 呼吸内科 | 2026-04-01 | 2026-04-08 | 7天 | J98.4 | 查看详情
  ENC-20260115-001 | 住院 | 呼吸内科 | 2026-01-15 | 2026-01-18 | 3天 | J84.10 | 查看详情
  ENC-20250920-001 | 门诊 | 内科     | 2025-09-20 | - | - | J06.9 | 查看详情
  ```
- 点击"查看详情"进入就诊详情页（已有的 EFPi5 页面）

---

### 8.3 新增弹窗（3个）

#### 8.3.1 脱敏预览弹窗（宽度 600px）

```
┌─ 脱敏预览 ─────────────────────────────────────────────┐
│                                                         │
│ 选择患者: [P20260408001 ▼]  张** / 男 / 58岁             │
│                                                         │
│ ── 预览对比 ────────────────────────────────────────── │
│ 字段       │ 原始值           │ 脱敏后                  │
│ 姓名       │ 张三丰           │ 张**                    │
│ 身份证     │ 310101196503123456│ 3101**********3456     │
│ 手机号     │ 13812345678      │ 138****5678             │
│ 出生日期   │ 1965-03-12       │ 1965年                  │
│ 住址       │ XX市XX区XX路...号 │ XX市XX区                │
│                                                         │
│ [确认导出] 需提交审批                                     │
│                                                         │
│                      [取消]  [提交导出申请]               │
└─────────────────────────────────────────────────────────┘
```

#### 8.3.2 导出审批弹窗（宽度 520px）

```
┌─ 数据导出申请 ─────────────────────────────────────────┐
│                                                         │
│ 导出范围 * [肺癌早筛队列 ▼]                               │
│ 数据类型   [✓] 基本信息 [✓] 诊断 [✓] 检验 [ ] 影像       │
│ 数据用途 * [          ] (必填，说明导出用途)               │
│ 预计数据量: 约 1,200 条患者记录                           │
│ 审批人   * [数据安全委员会 ▼]                             │
│                                                         │
│                      [取消]  [提交审批]                   │
└─────────────────────────────────────────────────────────┘
```

#### 8.3.3 新建映射规则弹窗（宽度 520px）

```
┌─ 新建映射规则 ─────────────────────────────────────────┐
│                                                         │
│ 源系统 *   [HIS ▼]                                       │
│ 源字段 *   [diagnosis_name ▼]                            │
│ 标准体系 * [ICD-10 ▼]                                    │
│ 目标编码 * [          ] (支持搜索编码/名称)               │
│ 映射类型   [自动 ▼] (自动/手动/正则)                      │
│ 置信度     [95] %                                        │
│ 备注       [          ]                                  │
│                                                         │
│                      [取消]  [保存规则]                   │
└─────────────────────────────────────────────────────────┘
```

---

## 9. 后续模块计划

完成模型管理 + CDR 数据管理模块后，按以下顺序继续：

1. **RDR 科研数据管理**（研究项目/队列/数据集/ETL/数据质量）
2. **系统设置模块**（6页+5弹窗）
3. **标注管理模块**（2页+1弹窗，需大幅扩展）
4. **任务调度+审计+告警+消息**（各2页，相对轻量）
5. **登录+Dashboard**（收尾优化）
